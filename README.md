# flat-inbox
A minimal, storage‑agnostic message inbox for static websites. Uses flat‑file or database backends, exposes a tiny API, and includes a secure admin UI with no external services required. Perfect for GitHub Pages, small sites, and low‑maintenance deployments.
